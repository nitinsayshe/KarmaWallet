<style>
  body {
    width: 100%;
    margin: 0;
    padding: 0;
  }

  h3, p, a, button {
    font-family: Arial;
  }

  p {
    font-size: 16px;
    font-style: normal;
    color: {{colors.dark}};
  }

  span {
    color: {{colors.dark}};
  }

  h3 {
    font-size: 18px;
    font-weight: bold;
    color: {{colors.dark}};
  }

  ul {
    margin-left: 0;
    padding-left: 0;
  }

  ol, li {
    margin-bottom: 10px;
    margin-left: 0;
    padding-left: 10px;
    font-size: 16px;
    font-style: normal;
    font-weight: 500;
    line-height: 26px;
    color: {{colors.dark}};
  }

  .bold {
    font-weight: 700 !important;
  }

  .italic {
    font-style: italic;
  }

  .uppercase {
    text-transform: uppercase;
    font-weight: 700;
  }

  .email-wrapper {
    width: 600px;
    margin: 0 auto;
    padding: 40px;
    background-color: {{colors.blue}};
  }

  .email-container {
    margin: 0 auto;
    width: 100%;
  }

  .logo-container {
    margin: 0 auto 40px;
    text-align: center;
  }

  .karma-logo {
    max-width: 150px;
    width: 150px;
  }

  .card {
    margin: 0 auto 40px;
    background-color: {{colors.white}};
    padding: 20px;
    border-radius: 8px;
    width: 90%;
  }

  .salutation-text {
    color: {{colors.blue}};
    font-size: 18px;
    font-style: normal;
    font-weight: 600;
    line-height: 26px;
  }

  .body-text {
    font-size: 16px;
    font-style: normal;
    font-weight: 500;
    line-height: 26px;
    margin-bottom: 24px;
    color: {{colors.dark}};
  }

  .body-text a {
    color: {{colors.blue}};
    text-decoration-line: underline;
    text-decoration-color: {{colors.blue}};
  }

  .support-text {
    margin-bottom: 20px;
    font-size: 14px;
    font-style: normal;
    font-weight: 400;
    line-height: 26px;
  }

  .support-text a {
    color: {{colors.blue}} !important;
    font-size: 14px;
    font-style: normal;
    font-weight: 400;
    line-height: 26px;
    text-decoration-line: underline;
    text-decoration-color: {{colors.blue}};
  }

  .disclaimer-text {
    color: {{colors.dark}};
    font-size: 12px;
    font-style: normal;
    font-weight: 400;
    line-height: 26px;
  }

  .cta-container {
    display: flex;
    justify-content: center;
  }

  .cta-link {
    border-radius: 70px;
    cursor: pointer !important;
    font-size: 14px;
    font-weight: 600;
    line-height: 20px;
    padding: 11px 25px;
    text-align: center;
    text-decoration: none;
    width: auto;
    color: {{colors.white}};
    background-color: {{colors.blue}};
  }

  .cta-container a {
    color: {{colors.white}};
  }

  a.inline-link {
    color: {{colors.blue}};
    text-decoration-line: underline;
    text-decoration-color: {{colors.blue}};
  }

  .dispute-signature-container .body-text {
    margin-bottom: 10px;
    font-size: 14px;
    line-height: 16px;
    font-style: italic;
  }
</style>
