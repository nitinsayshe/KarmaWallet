<div class="card">
    <p class="salutation-text">Hi {{name}},</p>
    {{#if (eq isSuccess true)}}
      <p class="body-text">Good news! Your card is linked and your transactions have been processed. You can now access your pesonal Karma Score, get data insights around your spending habits, earn cashback, and offset your shopping carbon footprint.</p>
      <div class="cta-container">
        <a class="cta-link" href="{{domain}}/transactions">View Impact</a>
      </div>
    {{/if}}
    {{#if (ne isSuccess true)}}
      <p class="body-text">We've had an issue linking your card and analyzing your transactions. Sign into your Karma Wallet account to re-link your card.</p>
      <div class="cta-container">
        <a class="cta-link" href="{{domain}}/account">View Account</a>
      </div>
    {{/if}}
</div>
   