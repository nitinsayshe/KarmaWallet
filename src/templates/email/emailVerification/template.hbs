  <div class="card">
      <p class="salutation-text">Hi {{name}},</p>
        {{#if (ne groupName undefined)}}
          <p class="body-text">Thanks for your request to join the <span class="bold">{{groupName}}</span> group. To
          complete your request, click the button below to verify your email address.
          </p>
        {{/if}}

        {{#if (eq groupName undefined)}}
          <p class="body-text">You're almost ready to get started! Please click the button below to verify your email address.</p>
        {{/if}}
  
      <div class="cta-container">
        <a class="cta-link" href="{{verificationLink}}">Verify Email</a>
      </div>
      <p class="support-text">
        If clicking the button above doesn't work, please copy and paste <a href="{{verificationLink}}">{{verificationLink}}</a> in a new browser window instead.
      </p>  
    </div>
